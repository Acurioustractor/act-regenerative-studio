// `server-only` is a Next.js build-time marker with no runtime behaviour; it is
// not resolvable outside the Next bundler. Aliased here so modules that import
// it (the editorial lib, and anything reading it) can be unit tested.
export {};
